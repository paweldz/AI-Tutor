# Supabase (required for auth + cloud sync)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Vercel serverless env vars (set in Vercel dashboard, not here)
# ANTHROPIC_API_KEY=sk-ant-...
# SUPABASE_URL=https://your-project.supabase.co   (for /api/db legacy proxy)
# SUPABASE_ANON_KEY=your-anon-key                 (for /api/db legacy proxy)
